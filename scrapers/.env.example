# Copy to scrapers/.env (never commit scrapers/.env)
# Generate a strong random value, e.g.: python -c "import secrets; print(secrets.token_hex(32))"
SCRAPER_API_KEY=replace_with_a_long_random_secret
